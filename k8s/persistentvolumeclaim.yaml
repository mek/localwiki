apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wiki-data-pvc
  namespace: wiki
  labels:
    app: personal-wiki
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path  # Rancher Desktop default storage class